# Dev Container 项目组管理器

一个用于批量打开 Dev Container 项目并支持分组管理的 VS Code 扩展。

## 功能特性

- 📦 **自动检测 Dev Container 项目**：自动扫描 Project Manager 中包含 `.devcontainer` 配置的项目
- ✅ **树形视图勾选**：在可视化树形视图中勾选/取消勾选项目
- 📁 **分组管理**：将选中的项目保存为命名组，方便快速访问
- 🚀 **批量打开**：支持可配置延迟的批量打开多个 Dev Container 项目
- 🔄 **一键重载所有窗口**：一键重新加载所有 Dev Container 窗口
- 🎯 **默认 "all" 组**：自动创建包含所有项目的 "all" 组

## 使用方法

### 1. 查看项目

点击活动栏中的 Dev Container Groups 图标，可以看到：
- **Projects（项目）**：来自 Project Manager 的所有 Dev Container 项目
- **Saved Groups（保存的组）**：你保存的项目组

### 2. 选择项目

在树形视图中点击项目来切换选中状态（✓ 表示已选中）。

### 3. 保存为组

1. 选择你想要分组的项目
2. 点击工具栏中的保存图标（💾）
3. 输入组名（例如："microservices-order"）
4. 组会被保存并显示在 "Saved Groups" 中

### 4. 打开项目

**打开选中的项目：**
- 在树形视图中选择项目
- 点击工具栏中的文件夹图标（📂）
- 确认打开所有选中的项目

**打开保存的组：**
- 点击 "Saved Groups" 中的组
- 或右键选择 "Open Group"
- 确认打开组中的所有项目

### 5. 重载所有窗口

点击重载图标（🔄）一次性重新加载所有 Dev Container 窗口。

## 配置选项

```json
{
  // 每个项目打开之间的延迟（毫秒）
  "devContainerGroups.openDelay": 2000,

  // 每批打开的项目数量
  "devContainerGroups.batchSize": 3,

  // 批次之间的延迟（毫秒）
  "devContainerGroups.batchDelay": 10000,

  // Project Manager 配置文件的自定义路径
  "devContainerGroups.projectManagerPath": ""
}
```

## 依赖要求

- [Project Manager](https://marketplace.visualstudio.com/items?itemName=alefragnani.project-manager) 扩展
- 项目必须包含 `.devcontainer/devcontainer.json` 或 `.devcontainer/docker-compose.yml`

## 使用技巧

- **快速打开**：减少 `openDelay` 和 `batchDelay` 可以更快打开（可能导致资源问题）
- **稳定打开**：如果容器启动失败，增加延迟时间
- **默认组**："all" 组会自动创建且不能删除
- **组织分组**：按功能创建组（例如："frontend"、"backend"、"microservices"）

## 命令列表

- `Dev Container Groups: Refresh Projects` - 从 Project Manager 重新加载项目
- `Dev Container Groups: Open Selected Projects` - 打开所有选中的项目
- `Dev Container Groups: Save as Group` - 将选中的项目保存为组
- `Dev Container Groups: Open Group` - 打开组中的所有项目
- `Dev Container Groups: Delete Group` - 删除保存的组
- `Dev Container Groups: Reload All Dev Container Windows` - 重新加载所有远程窗口

## 已知问题

- 同时打开太多项目可能导致 Docker 资源问题
- 容器启动时间因镜像大小和系统资源而异

## 更新日志

### 0.0.1

初始版本，包含核心功能：
- 项目检测和树形视图
- 分组管理
- 支持可配置延迟的批量打开
- 重载所有窗口功能
