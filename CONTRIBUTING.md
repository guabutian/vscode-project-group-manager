# 贡献指南

感谢你考虑为 Dev Container 项目组管理器做出贡献！

## 行为准则

### 我们的承诺

为了营造一个开放和友好的环境，我们承诺：

- 使用友好和包容的语言
- 尊重不同的观点和经验
- 优雅地接受建设性批评
- 关注对社区最有利的事情
- 对其他社区成员表示同理心

## 如何贡献

### 报告 Bug

在提交 Bug 报告之前：

1. **检查现有 Issues** - 确保问题尚未被报告
2. **使用最新版本** - 确认问题在最新版本中仍然存在
3. **收集信息** - 准备详细的复现步骤

提交 Bug 报告时，请包含：

```markdown
**描述问题**
简要描述 bug 是什么。

**复现步骤**
1. 打开 '...'
2. 点击 '...'
3. 滚动到 '...'
4. 看到错误

**期望行为**
描述你期望发生什么。

**实际行为**
描述实际发生了什么。

**截图**
如果适用，添加截图帮助解释问题。

**环境信息**
- OS: [例如 macOS 13.0]
- VS Code 版本: [例如 1.80.0]
- 插件版本: [例如 0.0.1]
- Docker 版本: [例如 24.0.0]

**额外信息**
添加任何其他相关信息。
```

### 建议新功能

在提交功能建议之前：

1. **检查现有 Issues** - 确保功能尚未被建议
2. **考虑范围** - 确保功能符合项目目标
3. **提供用例** - 说明为什么需要这个功能

提交功能建议时，请包含：

```markdown
**功能描述**
简要描述你想要的功能。

**使用场景**
描述这个功能解决什么问题。

**建议的实现**
如果有想法，描述如何实现。

**替代方案**
描述你考虑过的其他解决方案。

**额外信息**
添加任何其他相关信息或截图。
```

### 提交代码

#### 开发流程

1. **Fork 项目**
   ```bash
   # 在 GitHub 上点击 Fork 按钮
   ```

2. **克隆仓库**
   ```bash
   git clone https://github.com/your-username/dev-container-group-manager.git
   cd dev-container-group-manager
   ```

3. **创建分支**
   ```bash
   git checkout -b feature/amazing-feature
   # 或
   git checkout -b fix/bug-fix
   ```

4. **安装依赖**
   ```bash
   npm install
   ```

5. **开发和测试**
   ```bash
   # 编译代码
   npm run compile

   # 监听模式
   npm run watch

   # 代码检查
   npm run lint

   # 按 F5 在 VS Code 中调试
   ```

6. **提交更改**
   ```bash
   git add .
   git commit -m "feat: 添加新功能"
   ```

7. **推送到 GitHub**
   ```bash
   git push origin feature/amazing-feature
   ```

8. **创建 Pull Request**
   - 在 GitHub 上打开你的 fork
   - 点击 "New Pull Request"
   - 填写 PR 描述

#### 提交信息规范

使用 [约定式提交](https://www.conventionalcommits.org/zh-hans/) 格式：

```
<类型>(<范围>): <描述>

[可选的正文]

[可选的脚注]
```

**类型**：
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构（既不是新功能也不是 Bug 修复）
- `perf`: 性能优化
- `test`: 添加测试
- `chore`: 构建过程或辅助工具的变动

**示例**：
```bash
feat(groups): 添加组编辑功能
fix(projects): 修复项目路径解析错误
docs(readme): 更新安装说明
style(extension): 格式化代码
refactor(manager): 重构项目管理器
perf(open): 优化项目打开速度
test(groups): 添加组管理测试
chore(deps): 更新依赖版本
```

#### 代码规范

**TypeScript 规范**：

```typescript
// ✅ 好的代码
export class ProjectManager {
    private projects: Project[] = [];

    constructor(private context: vscode.ExtensionContext) {
        this.loadProjects();
    }

    async loadProjects(): Promise<void> {
        // 实现...
    }
}

// ❌ 不好的代码
export class ProjectManager {
    projects: any;

    constructor(context) {
        this.projects = [];
    }

    loadProjects() {
        // 实现...
    }
}
```

**注释规范**：

```typescript
// ✅ 好的注释（中文）
/**
 * 加载所有 Dev Container 项目
 * 从 Project Manager 配置中读取项目列表，
 * 并过滤出包含 .devcontainer 的项目
 */
async loadProjects(): Promise<void> {
    // 尝试从 Project Manager 加载项目
    const projectManagerProjects = await this.loadFromProjectManager();

    // 只保留包含 .devcontainer 的项目
    for (const project of projectManagerProjects) {
        if (this.hasDevContainer(project.path)) {
            this.projects.push({
                ...project,
                hasDevContainer: true
            });
        }
    }
}

// ❌ 不好的注释
// load projects
async loadProjects(): Promise<void> {
    // get projects
    const projectManagerProjects = await this.loadFromProjectManager();

    // filter
    for (const project of projectManagerProjects) {
        if (this.hasDevContainer(project.path)) {
            this.projects.push({
                ...project,
                hasDevContainer: true
            });
        }
    }
}
```

**命名规范**：

```typescript
// ✅ 好的命名
class ProjectManager { }
interface Project { }
const projectPath: string;
function loadProjects(): void { }

// ❌ 不好的命名
class projectmanager { }
interface project { }
const pp: string;
function load(): void { }
```

#### 测试要求

在提交 PR 之前，请确保：

1. **手动测试所有功能**
   - 项目检测
   - 项目选择
   - 组保存
   - 组打开
   - 组删除
   - 窗口重载

2. **测试不同场景**
   - 没有项目
   - 单个项目
   - 多个项目
   - 大量项目（>10个）

3. **测试错误处理**
   - Project Manager 未安装
   - Docker 未运行
   - 无效的项目路径
   - 网络错误

4. **测试不同配置**
   - 不同的延迟设置
   - 不同的批次大小
   - 自定义路径

#### Pull Request 检查清单

在提交 PR 之前，请确认：

- [ ] 代码遵循项目的编码规范
- [ ] 添加了适当的注释（中文）
- [ ] 更新了相关文档
- [ ] 在 CHANGELOG.md 中记录了变更
- [ ] 手动测试了所有功能
- [ ] 测试了错误场景
- [ ] 代码通过了 ESLint 检查
- [ ] TypeScript 编译无错误
- [ ] PR 描述清晰完整

#### Pull Request 模板

```markdown
## 变更类型
- [ ] Bug 修复
- [ ] 新功能
- [ ] 重大变更
- [ ] 文档更新

## 描述
简要描述这个 PR 做了什么。

## 相关 Issue
关闭 #(issue 编号)

## 变更内容
- 添加了...
- 修复了...
- 更新了...

## 测试
描述你如何测试这些变更。

## 截图
如果适用，添加截图。

## 检查清单
- [ ] 代码遵循编码规范
- [ ] 添加了注释
- [ ] 更新了文档
- [ ] 更新了 CHANGELOG
- [ ] 手动测试通过
- [ ] ESLint 检查通过
- [ ] TypeScript 编译通过
```

## 开发环境设置

### 必需工具

- Node.js 20.x
- npm 9.x
- VS Code 1.80+
- Git
- Docker Desktop

### 推荐扩展

- ESLint
- TypeScript and JavaScript Language Features
- Project Manager

### 项目结构

```
dev-container-group-manager/
├── src/                      # 源代码
│   ├── extension.ts          # 插件入口
│   ├── projectManager.ts     # 项目管理
│   ├── groupManager.ts       # 组管理
│   ├── projectsTreeProvider.ts
│   └── groupsTreeProvider.ts
├── out/                      # 编译输出
├── node_modules/             # 依赖
├── .vscode/                  # VS Code 配置
├── package.json              # 插件配置
├── tsconfig.json             # TypeScript 配置
└── README.md                 # 文档
```

### 调试技巧

1. **使用断点**
   ```typescript
   // 在代码中设置断点
   async loadProjects(): Promise<void> {
       debugger; // 或在编辑器中点击行号设置断点
       const projects = await this.loadFromProjectManager();
   }
   ```

2. **查看日志**
   ```typescript
   console.log('项目列表:', this.projects);
   console.error('加载失败:', error);
   ```

3. **使用 Output 面板**
   - View > Output
   - 选择 "Extension Host"

4. **使用开发者工具**
   - Help > Toggle Developer Tools

## 代码审查流程

### 审查者指南

审查 PR 时，请检查：

1. **代码质量**
   - 是否遵循编码规范
   - 是否有适当的注释
   - 是否有明显的 bug

2. **功能完整性**
   - 是否实现了所有功能
   - 是否处理了错误情况
   - 是否考虑了边界情况

3. **性能影响**
   - 是否有性能问题
   - 是否有内存泄漏
   - 是否有不必要的计算

4. **文档更新**
   - 是否更新了 README
   - 是否更新了 CHANGELOG
   - 是否添加了必要的注释

5. **测试覆盖**
   - 是否测试了主要功能
   - 是否测试了错误场景
   - 是否测试了边界情况

### 审查反馈

提供反馈时：

- 使用友好和建设性的语言
- 解释为什么需要改变
- 提供具体的建议
- 认可好的代码

**示例**：

```markdown
✅ 好的反馈：
"这里可以使用 `Array.filter()` 来简化代码，这样更易读：
```typescript
const devContainerProjects = projects.filter(p => this.hasDevContainer(p.path));
```

❌ 不好的反馈：
"这段代码写得不好，重写。"
```

## 发布流程

### 版本号规则

遵循 [语义化版本](https://semver.org/lang/zh-CN/)：

- **主版本号**：不兼容的 API 变更
- **次版本号**：向下兼容的功能新增
- **修订号**：向下兼容的问题修正

### 发布步骤

1. **更新版本号**
   ```bash
   npm version patch  # 0.0.1 -> 0.0.2
   npm version minor  # 0.0.1 -> 0.1.0
   npm version major  # 0.0.1 -> 1.0.0
   ```

2. **更新 CHANGELOG**
   ```markdown
   ## [0.1.0] - 2026-02-01

   ### 新增
   - 组编辑功能

   ### 修复
   - 修复项目路径解析错误
   ```

3. **提交变更**
   ```bash
   git add .
   git commit -m "chore: 发布 v0.1.0"
   git tag v0.1.0
   git push origin main --tags
   ```

4. **打包和发布**
   ```bash
   vsce package
   vsce publish
   ```

## 社区

### 获取帮助

- 查看文档：README.md, QUICKSTART.md, EXAMPLES.md
- 搜索现有 Issues
- 创建新的 Issue

### 讨论

- 功能建议
- 最佳实践
- 使用技巧

## 许可证

通过贡献代码，你同意你的贡献将在 MIT 许可证下发布。

## 致谢

感谢所有贡献者！你们的贡献让这个项目变得更好。

---

再次感谢你的贡献！🎉
