# 使用示例和截图说明

## 界面预览

### 主界面布局

```
┌─────────────────────────────────────────┐
│  VS Code 活动栏                          │
│  ├─ 文件浏览器                           │
│  ├─ 搜索                                 │
│  ├─ 源代码管理                           │
│  ├─ 运行和调试                           │
│  └─ 🖥️  Dev Container Groups (新增)     │
└─────────────────────────────────────────┘
```

点击 Dev Container Groups 图标后：

```
┌─────────────────────────────────────────┐
│  DEV CONTAINER GROUPS                    │
│                                          │
│  ▼ PROJECTS                    🔄 📂 💾 🔁│
│    ✓ order-service                       │
│    ✓ payment-service                     │
│    ○ user-service                        │
│    ✓ notification-service                │
│    ○ api-gateway                         │
│                                          │
│  ▼ SAVED GROUPS                          │
│    📚 all (5 个项目)                     │
│    📁 order-feature (3 个项目)           │
│    📁 user-feature (2 个项目)            │
└─────────────────────────────────────────┘
```

图标说明：
- 🔄 刷新项目列表
- 📂 打开选中的项目
- 💾 保存为组
- 🔁 重载所有窗口
- ✓ 已选中的项目
- ○ 未选中的项目
- 📚 "all" 组（特殊图标）
- 📁 普通组

## 完整使用流程

### 1. 首次使用

#### 步骤 1：安装依赖
确保已安装：
- ✅ VS Code
- ✅ Docker Desktop
- ✅ Project Manager 扩展
- ✅ Dev Containers 扩展

#### 步骤 2：配置 Project Manager
在 Project Manager 中添加你的项目：
1. 打开命令面板（Cmd/Ctrl + Shift + P）
2. 输入 "Project Manager: Save Project"
3. 为每个 Dev Container 项目保存

#### 步骤 3：启动插件
1. 按 F5 启动调试
2. 在新窗口中点击 Dev Container Groups 图标
3. 插件会自动扫描并显示所有 Dev Container 项目

### 2. 日常使用

#### 场景 A：开发新功能

**需求**：开发订单支付功能，需要同时打开 3 个服务

**操作流程**：
```
1. 点击 Dev Container Groups 图标
   └─ 打开侧边栏

2. 在 Projects 视图中勾选项目
   ├─ 点击 order-service ✓
   ├─ 点击 payment-service ✓
   └─ 点击 notification-service ✓

3. 保存为组
   ├─ 点击工具栏的 💾 图标
   ├─ 输入组名："order-payment-feature"
   └─ 点击确认

4. 打开项目
   ├─ 点击工具栏的 📂 图标
   ├─ 确认对话框：点击"是"
   └─ 等待进度条完成

5. 结果
   └─ 3 个新的 VS Code 窗口打开
      ├─ 窗口 1: order-service (Dev Container)
      ├─ 窗口 2: payment-service (Dev Container)
      └─ 窗口 3: notification-service (Dev Container)
```

**预期时间**：
- 快速模式：约 15-30 秒
- 稳定模式：约 30-60 秒

#### 场景 B：切换到另一个功能

**需求**：从订单功能切换到用户功能

**操作流程**：
```
1. 关闭当前所有项目窗口
   └─ 点击 🔁 重载所有窗口图标
      └─ 确认对话框：点击"是"

2. 打开新的项目组
   ├─ 在 Saved Groups 中找到 "user-feature"
   ├─ 点击组名
   └─ 确认对话框：点击"是"

3. 结果
   └─ 新的项目窗口打开
```

#### 场景 C：临时打开单个项目

**需求**：快速查看某个项目的代码

**操作流程**：
```
1. 在 Projects 视图中找到项目
2. 勾选该项目
3. 点击 📂 打开
4. 查看完毕后关闭窗口
```

### 3. 高级使用

#### 技巧 1：使用 "all" 组

"all" 组包含所有检测到的 Dev Container 项目：

```
使用场景：
- 需要查看所有项目的整体架构
- 进行全局搜索或重构
- 检查所有项目的依赖版本

操作：
1. 点击 Saved Groups 中的 "all"
2. 确认打开所有项目
3. 等待所有窗口打开完成
```

**注意**：如果项目很多（>10个），建议调整配置：
```json
{
  "devContainerGroups.batchSize": 2,
  "devContainerGroups.batchDelay": 20000
}
```

#### 技巧 2：按功能模块组织

推荐的组织方式：

```
项目组结构：
├─ all (所有项目)
├─ frontend (前端项目)
│  ├─ web-app
│  ├─ admin-dashboard
│  └─ mobile-app
├─ backend (后端服务)
│  ├─ api-gateway
│  ├─ auth-service
│  └─ user-service
├─ microservices-order (订单相关)
│  ├─ order-service
│  ├─ payment-service
│  └─ notification-service
└─ infrastructure (基础设施)
   ├─ monitoring
   └─ logging
```

#### 技巧 3：快速重新加载

当容器配置更新后：

```
1. 点击 🔁 重载所有窗口
2. 所有 Dev Container 窗口会重新加载
3. 容器会使用新的配置重新构建
```

## 配置优化

### 根据项目数量调整

**小型项目（1-3 个项目）**：
```json
{
  "devContainerGroups.openDelay": 1000,
  "devContainerGroups.batchSize": 3,
  "devContainerGroups.batchDelay": 5000
}
```

**中型项目（4-8 个项目）**：
```json
{
  "devContainerGroups.openDelay": 2000,
  "devContainerGroups.batchSize": 3,
  "devContainerGroups.batchDelay": 10000
}
```

**大型项目（9+ 个项目）**：
```json
{
  "devContainerGroups.openDelay": 3000,
  "devContainerGroups.batchSize": 2,
  "devContainerGroups.batchDelay": 15000
}
```

### 根据机器性能调整

**高性能机器**（16GB+ RAM, SSD）：
```json
{
  "devContainerGroups.openDelay": 1000,
  "devContainerGroups.batchSize": 5,
  "devContainerGroups.batchDelay": 5000
}
```

**普通机器**（8GB RAM）：
```json
{
  "devContainerGroups.openDelay": 2000,
  "devContainerGroups.batchSize": 3,
  "devContainerGroups.batchDelay": 10000
}
```

**低配机器**（<8GB RAM）：
```json
{
  "devContainerGroups.openDelay": 4000,
  "devContainerGroups.batchSize": 1,
  "devContainerGroups.batchDelay": 20000
}
```

## 故障排除

### 问题 1：项目列表为空

**症状**：Projects 视图中没有显示任何项目

**可能原因**：
1. Project Manager 未安装
2. 项目没有 `.devcontainer` 配置
3. Project Manager 配置路径错误

**解决方案**：
```bash
# 1. 检查 Project Manager 是否安装
code --list-extensions | grep alefragnani.project-manager

# 2. 检查项目是否有 .devcontainer
ls -la /path/to/project/.devcontainer

# 3. 手动指定配置路径
# 在 VS Code 设置中添加：
{
  "devContainerGroups.projectManagerPath": "/path/to/projects.json"
}
```

### 问题 2：打开项目失败

**症状**：点击打开后没有新窗口，或显示错误

**可能原因**：
1. Docker 未运行
2. 容器配置错误
3. 资源不足

**解决方案**：
```bash
# 1. 检查 Docker 状态
docker ps

# 2. 检查 Docker 资源
docker stats

# 3. 手动测试打开项目
# 在 VS Code 中：
# Cmd/Ctrl + Shift + P
# 输入 "Dev Containers: Open Folder in Container"
# 选择项目目录
```

### 问题 3：容器启动慢

**症状**：打开项目后等待很久才能使用

**可能原因**：
1. 镜像需要下载
2. 依赖需要安装
3. 网络慢

**解决方案**：
```bash
# 1. 预先拉取镜像
docker pull mcr.microsoft.com/devcontainers/base:ubuntu

# 2. 使用本地镜像源
# 在 devcontainer.json 中配置：
{
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "VARIANT": "ubuntu-22.04"
    }
  }
}

# 3. 增加延迟时间
{
  "devContainerGroups.openDelay": 5000,
  "devContainerGroups.batchDelay": 30000
}
```

### 问题 4：组保存后找不到

**症状**：保存的组在 Saved Groups 中不显示

**可能原因**：
1. 保存失败
2. VS Code 异常退出
3. globalState 被清除

**解决方案**：
```bash
# 1. 重新保存组
# 2. 检查 VS Code 日志
# Output > Extension Host

# 3. 手动检查存储
# 组数据保存在 VS Code 的 globalState 中
# 可以通过开发者工具查看
```

## 最佳实践

### 1. 组命名规范

推荐的命名方式：

```
✅ 好的命名：
- order-payment-feature
- user-auth-module
- frontend-all
- backend-microservices

❌ 不好的命名：
- test
- 123
- 临时
- aaa
```

### 2. 定期更新 "all" 组

当添加新项目后：

```
1. 点击 🔄 刷新项目列表
2. 在 Projects 中选择所有项目
3. 保存为 "all" 组（覆盖旧的）
```

### 3. 使用描述性组名

```
项目：订单服务 + 支付服务 + 通知服务

✅ 好的组名：
- order-payment-notification
- order-feature-complete
- payment-flow

❌ 不好的组名：
- group1
- test
- 新建组
```

### 4. 合理控制组的大小

```
推荐：
- 小组：2-3 个项目（快速开发）
- 中组：4-6 个项目（功能模块）
- 大组：7-10 个项目（完整系统）

不推荐：
- 超大组：>10 个项目（资源消耗大）
```

## 键盘快捷键（未来功能）

计划支持的快捷键：

```
Cmd/Ctrl + Shift + G        打开 Dev Container Groups 视图
Cmd/Ctrl + Shift + O        打开选中的项目
Cmd/Ctrl + Shift + S        保存为组
Cmd/Ctrl + Shift + R        重载所有窗口
```

## 与其他工具集成

### 与 Git 集成

```bash
# 批量拉取所有项目的最新代码
for project in order-service payment-service notification-service; do
  cd /path/to/$project
  git pull
done

# 然后打开项目组
```

### 与 Docker Compose 集成

```yaml
# docker-compose.yml
version: '3.8'
services:
  order-service:
    build: ./order-service
  payment-service:
    build: ./payment-service
  notification-service:
    build: ./notification-service
```

### 与 Makefile 集成

```makefile
# Makefile
.PHONY: start-order-feature
start-order-feature:
	@echo "Starting order feature services..."
	docker-compose up -d order-service payment-service notification-service
	@echo "Opening projects in VS Code..."
	# 使用插件打开项目组
```

## 总结

这个插件的核心价值：

1. **提高效率**：一键打开多个相关项目
2. **减少错误**：不会忘记打开某个依赖服务
3. **标准化流程**：团队成员使用相同的项目组
4. **灵活配置**：根据需求调整打开策略

开始使用吧！🚀
